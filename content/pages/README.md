# content/pages

One markdown file per page: `homepage.md`, `about.md`, `the-method.md`, `the-reset.md`,
`speaking.md`, `books.md`, `contact.md`, `blog-resources.md`, `privacy-policy.md`. This list
supersedes the original guess in this file (originally "homepage, about, speaking, coaching,
book, contact") now that the real sitemap has been confirmed, see `docs/SITEMAP.md`.

Populated on 2026-08-13 with Vaida's own already-approved copy, pulled from her Google Docs
(see `docs/BUILD_LOG.md` for sources and status of each page). Two pages (`about.md`,
`contact.md`) were retrieved as AI-generated summaries rather than word-for-word text and need
re-confirming. `the-method.md` is missing its final copy entirely, only the structural handoff
was available, see `docs/BUILD_LOG.md`.

See `docs/BRAND_CONTEXT.md` for voice and non-negotiables before editing anything here.
