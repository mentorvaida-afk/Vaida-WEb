# src/components

Core, brand-token-driven UI components live here (buttons, headings, the booking callout, the
email capture form). Built in Stage 03 (`stages/03-design-system/STAGE.md`). Nothing goes here
until that stage's design plan is reviewed and approved — see the `frontend-design` skill
process referenced in that stage file.
