# src/lib

Integrations and utilities: Calendly embed helper, email capture API call, analytics
initialisation. Built in Stage 06 (`stages/06-conversion-architecture/STAGE.md`). No API keys or
secrets in this folder — read them from environment variables only (see `.env.example`).
