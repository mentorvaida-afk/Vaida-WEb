# Stage 06 — Conversion architecture

**In**: The built pages from Stage 04.

**Do**: Wire in the real Calendly embed and the real email capture flow. Add spam protection.
Add graceful fallbacks for when either service is down. Add analytics.

**Out**: fully functional booking and email capture, tested with real (not fake) submissions in
a staging environment.

**Done when**: checks 7, 8, 9, 10, 11 in `docs/INSPECTION_CHECKLIST.md` pass.

**Do not look at**: copy tone, visual design — function only, using what Stages 03–05 already
built.
