# Stage 03 — Design system

**In**: `docs/BRAND_CONTEXT.md`, `docs/PAGE_BLUEPRINTS.md`, the `frontend-design` studio process
(brainstorm a token system, review against the brief for anything generic, then build).

**Do**: Turn the brand tokens into an actual Tailwind config and a small set of core components
(buttons, headings, cards, the booking callout, the email capture form). Take one real,
justified aesthetic risk — see `frontend-design` skill for how to avoid the generic
cream-background-and-terracotta AI-design default.

**Out**: `tailwind.config.ts`, `src/components/` core set, a one-page style reference.

**Done when**: the core components could not be mistaken for a generic template, and every
colour/font traces back to `docs/BRAND_CONTEXT.md`.

**Do not look at**: page copy, hosting, forms logic — visual system only.
