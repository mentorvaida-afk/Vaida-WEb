# Stage 01 — Discovery

**In**: `docs/BRAND_CONTEXT.md`, `docs/USER_EXPERIENCE_FIRST.md`, Vaida's existing content
(golden sayings, book manuscript excerpts, speaker bio, prior website copy).

**Do**: Confirm the four user personas still hold. Confirm the two conversion paths (booking,
workbook) are still correct. List every page the v1 site needs, no more.

**Out**: `docs/SITEMAP.md` — a flat list of pages, each with its single job and its one primary
call to action.

**Done when**: every page on the list can be justified against a persona in
`USER_EXPERIENCE_FIRST.md`. If a page can't be, it's cut.

**Do not look at**: component code, styling, hosting — none of that exists yet, and none of it
should influence this stage.
