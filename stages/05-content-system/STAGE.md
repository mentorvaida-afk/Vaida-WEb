# Stage 05 — Content system

**In**: Vaida's golden sayings, speaker bio, book excerpts, existing approved copy.

**Do**: Populate `content/` with real, Vaida-approved copy for every page. No placeholder text
reaches Stage 04's build past this point. Structure golden sayings as individual markdown files
so new ones can be added without touching code.

**Out**: complete `content/` directory, reviewed line by line by Vaida.

**Done when**: check 3, 5, 6 and 13 in `docs/INSPECTION_CHECKLIST.md` all pass.

**Do not look at**: styling or layout — content correctness and voice only.
